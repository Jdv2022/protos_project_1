syntax = "proto3";

package getUserDetails;

option php_namespace = "grpc\\GetUserDetails";

message ProfileImageRequest {
	int64 fk = 1;
}

message ProfileImageResponse {
	bool profile_image = 1;
}

service ProfileImageService {
    rpc GetUserDetails(ProfileImageRequest) returns (ProfileImageResponse);
}